# 数据库配置模块独立化方案

## 概述

将数据库连接模块独立出来，支持通过配置文件在 SQLite 和 MySQL 之间切换。

## 目标

1. 数据库配置与代码解耦
2. 通过 `config.env` 灵活切换数据库类型
3. 支持 SQLite 和 MySQL 两种数据库
4. 初始化时提供数据库类型选择

---

## 方案设计

### 1. 文件结构

```
cimf_v2/
├── cimf_django/
│   ├── database.py      # 新增：数据库配置模块
│   ├── settings.py      # 修改：使用 database.py
│   └── ...
├── config.env.sample    # 新增：配置模板
├── config.env          # 由 sample 复制，运行时使用
├── .env                 # 删除：合并到 config.env
├── requirements.txt    # 修改：添加 pymysql
└── run.sh              # 修改：添加数据库选择交互
```

### 2. config.env 配置格式

```bash
# ============================================
# CIMF 管理系统配置文件
# ============================================

# ----- Django 设置 -----
SECRET_KEY=your-secret-key-here
DEBUG=True

# ----- 数据库配置 -----
# 数据库类型: sqlite / mysql
DB_TYPE=sqlite

# 以下配置仅在 DB_TYPE=mysql 时生效
# DB_NAME=cimf
# DB_USER=root
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=3306
```

### 3. config.env.sample 配置格式

```bash
# ============================================
# CIMF 管理系统配置文件模板
# ============================================

# ----- Django 设置 -----
SECRET_KEY=your-secret-key-here
DEBUG=True

# ----- 数据库配置 -----
# 选择数据库类型: sqlite 或 mysql
# 使用 SQLite（默认）
DB_TYPE=sqlite
# 使用 MySQL 时，取消注释下方配置
# DB_TYPE=mysql
# DB_NAME=cimf
# DB_USER=root
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=3306
```

### 4. database.py 模块设计

```python
# -*- coding: utf-8 -*-
"""
数据库配置模块

根据 config.env 配置返回 Django DATABASES 格式的数据库配置
支持 SQLite 和 MySQL 两种数据库
"""

import os
from pathlib import Path

# 项目根目录
BASE_DIR = Path(__file__).resolve().parent.parent


def get_database_config():
    """
    根据配置获取数据库配置
    
    读取 config.env 中的 DB_TYPE、DB_NAME、DB_USER 等配置
    返回 Django DATABASES 格式的配置字典
    
    Returns:
        dict: Django DATABASES 配置
    """
    
    # 读取配置文件
    config_path = BASE_DIR / 'config.env'
    db_config = _load_config(config_path)
    
    db_type = db_config.get('DB_TYPE', 'sqlite').lower().strip()
    
    if db_type == 'mysql':
        return _get_mysql_config(db_config)
    else:
        return _get_sqlite_config()


def _load_config(config_path: Path) -> dict:
    """
    读取配置文件
    
    Args:
        config_path: 配置文件路径
        
    Returns:
        dict: 配置字典
    """
    config = {}
    
    if not config_path.exists():
        return config
    
    with open(config_path, 'r', encoding='utf-8') as f:
        for line in f:
            line = line.strip()
            # 跳过注释和空行
            if not line or line.startswith('#'):
                continue
            
            if '=' in line:
                key, value = line.split('=', 1)
                config[key.strip()] = value.strip()
    
    return config


def _get_sqlite_config() -> dict:
    """获取 SQLite 配置"""
    return {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'instance' / 'django.db',
    }


def _get_mysql_config(config: dict) -> dict:
    """获取 MySQL 配置"""
    return {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': config.get('DB_NAME', 'cimf'),
        'USER': config.get('DB_USER', 'root'),
        'PASSWORD': config.get('DB_PASSWORD', ''),
        'HOST': config.get('DB_HOST', 'localhost'),
        'PORT': config.get('DB_PORT', '3306'),
        'OPTIONS': {
            'charset': 'utf8mb4',
        },
    }
```

### 5. settings.py 修改

```python
# 文件顶部添加
try:
    import pymysql
    pymysql.install_as_MySQLdb()
except ImportError:
    pass

# ...

# Database 配置改为
from cimf_django.database import get_database_config
DATABASES = {
    'default': get_database_config()
}
```

### 6. requirements.txt 修改

```diff
 # 数据库
 # sqlite3 (built-in)
+# pymysql (可选，使用 MySQL 时安装)
+# pip install pymysql
```

### 7. run.sh 修改

在 `create_env_file()` 函数中添加数据库类型选择：

```bash
create_env_file() {
    echo -e "\n${GREEN}>>> 创建 config.env 文件${NC}\n"
    
    if [[ -f "config.env" ]]; then
        echo -e "${YELLOW}config.env 已存在${NC}"
        read -p "是否覆盖？(y/N) " answer
        if [[ ! "$answer" =~ ^[Yy]$ ]]; then
            echo "取消创建"
            return 0
        fi
    fi
    
    if [[ -f "config.env.sample" ]]; then
        # 选择数据库类型
        echo "请选择数据库类型："
        echo "  1 → SQLite（默认，适合开发和测试）"
        echo "  2 → MySQL（适合生产环境）"
        read -p "请输入选项 (1/2): " db_choice
        
        case "$db_choice" in
            1)
                # SQLite 配置
                sed 's/^# DB_TYPE=mysql/DB_TYPE=sqlite/' config.env.sample > config.env
                echo -e "${GREEN}已创建 config.env（SQLite）${NC}"
                ;;
            2)
                # MySQL 配置
                echo "请输入 MySQL 配置："
                read -p "  数据库名 [cimf]: " db_name
                read -p "  用户名 [root]: " db_user
                read -s -p "  密码: " db_pass; echo
                read -p "  主机 [localhost]: " db_host
                read -p "  端口 [3306]: " db_port
                
                # 生成配置
                cp config.env.sample config.env
                sed -i 's/^DB_TYPE=sqlite/DB_TYPE=mysql/' config.env
                sed -i "s/^# DB_NAME=cimf/DB_NAME=${db_name:-cimf}/" config.env
                sed -i "s/^# DB_USER=root/DB_USER=${db_user:-root}/" config.env
                sed -i "s/^# DB_PASSWORD=$/DB_PASSWORD=${db_pass}/" config.env
                sed -i "s/^# DB_HOST=localhost/DB_HOST=${db_host:-localhost}/" config.env
                sed -i "s/^# DB_PORT=3306/DB_PORT=${db_port:-3306}/" config.env
                
                # 取消注释 MySQL 配置行
                sed -i 's/^# \(DB_TYPE=mysql\)/\1/' config.env
                sed -i 's/^# \(DB_NAME=\)/\1/' config.env
                sed -i 's/^# \(DB_USER=\)/\1/' config.env
                sed -i 's/^# \(DB_PASSWORD=\)/\1/' config.env
                sed -i 's/^# \(DB_HOST=\)/\1/' config.env
                sed -i 's/^# \(DB_PORT=\)/\1/' config.env
                
                echo -e "${GREEN}已创建 config.env（MySQL）${NC}"
                ;;
            *)
                cp config.env.sample config.env
                echo -e "${YELLOW}使用默认配置（SQLite）${NC}"
                ;;
        esac
    else
        echo -e "${RED}config.env.sample 不存在${NC}"
    fi
}
```

---

## 实施步骤

### 步骤 1：创建数据库配置模块

创建 `cimf_django/database.py`：

- 实现 `get_database_config()` 函数
- 实现配置读取函数 `_load_config()`
- 实现 SQLite/MySQL 配置生成函数

### 步骤 2：修改 settings.py

- 文件顶部添加 pymysql 兼容性处理
- 导入 `get_database_config`
- `DATABASES` 改为调用函数

### 步骤 3：创建 config.env.sample

- 包含 SQLite 和 MySQL 配置段
- 注释掉 MySQL 部分作为默认

### 步骤 4：处理现有 .env 文件

将 `.env` 内容合并到 `config.env.sample`：

```bash
# .env 原内容
FLASK_ENV=development
SECRET_KEY=3rmKh2LcWwEEINlXtKQQQwMUd1k3Ucr43MdJ9b2YUmMDTArLUqbWrtb_IsUE-7e6
```

合并后 `config.env.sample` 应包含这些配置。

### 步骤 5：修改 requirements.txt

添加 pymysql 依赖说明（注释）。

### 步骤 6：修改 run.sh

在 `create_env_file()` 函数中添加数据库类型选择逻辑。

---

## 测试验证

### 功能测试

1. **SQLite 模式**
   - 选择 SQLite 配置
   - 运行 `python manage.py check`
   - 确认数据库正常连接

2. **MySQL 模式**
   - 选择 MySQL 配置
   - 输入 MySQL 连接信息
   - 运行 `python manage.py check`
   - 确认数据库正常连接

### 切换测试

1. 修改 `config.env` 中的 `DB_TYPE`
2. 重启服务
3. 确认切换成功

---

## 回滚方案

如有问题，可回滚到修改前状态：

1. 删除 `cimf_django/database.py`
2. 恢复 `settings.py` 中的 DATABASES 配置
3. 删除 `config.env` 和 `config.env.sample`
4. 恢复 `.env`

---

## 附录

### 相关文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `cimf_django/database.py` | 新建 | 数据库配置模块 |
| `cimf_django/settings.py` | 修改 | 导入 database 模块 |
| `config.env.sample` | 新建 | 配置模板 |
| `.env` | 删除 | 合并到 config.env |
| `requirements.txt` | 修改 | 添加依赖说明 |
| `run.sh` | 修改 | 添加数据库选择 |

### MySQL 依赖安装

```bash
# 使用 MySQL 时需要安装 pymysql
pip install pymysql
```