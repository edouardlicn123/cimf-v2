# 代码封装与重构优化计划

> 创建时间：2026-05-02
> 阶段：Stage 4
> 目标：消除重复代码、提升可维护性、统一项目规范

---

## 一、概述

经过对整个项目的系统审查，发现以下主要问题：

| 问题类型 | 影响范围 | 严重程度 |
|----------|----------|----------|
| 模型字段重复定义 | 6+ 个模型 | 中 |
| 视图分页逻辑重复 | 3+ 个视图 | 中 |
| 服务类缺少公共基类 | 10+ 个服务 | 中 |
| 表单控件样式硬编码 | 5+ 个表单 | 低 |
| 视图表单使用不一致 | 3+ 个视图 | 高 |
| 模板元素重复（分页、徽章） | 5+ 个模板 | 低 |
| 常量定义分散 | 3+ 处 | 低 |

---

## 二、优化方案

### 优化 1：抽象基础模型 `BaseModel`

**目标：** 消除 `created_at`、`updated_at` 在多个模型中的重复定义。

**当前问题：**
- `core/models.py` 中的 `Taxonomy`、`TaxonomyItem`、`ChinaRegion`
- `core/node/models.py` 中的 `NodeType`、`Node`、`Module`、`ToolType`
- `modules/customer/models.py` 中的 `CustomerFields`

均各自定义了相同的时间戳字段。

**实施方案：**

1. 在 `core/models.py` 中创建抽象基类：
```python
class BaseModel(models.Model):
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        abstract = True
```

2. 让所有模型继承 `BaseModel`，删除各自重复的字段定义。

3. 运行 `python manage.py makemigrations` 确认无迁移变化（abstract 不产生迁移）。

**涉及文件：**
- `core/models.py`
- `core/node/models.py`
- `modules/customer/models.py`

**风险评估：** 低风险，abstract 模型不改变数据库结构。

---

### 优化 2：分页工具函数

**目标：** 统一视图中的分页逻辑。

**当前问题：** `core/views/taxonomy.py`、`core/views/cron.py` 等视图中重复编写分页代码。

**实施方案：**

1. 创建 `core/utils/pagination.py`：
```python
from django.core.paginator import Paginator

def paginate_queryset(request, queryset, per_page=10):
    """通用分页工具函数"""
    try:
        page_num = int(request.GET.get('page', 1))
        if page_num < 1:
            page_num = 1
    except (ValueError, TypeError):
        page_num = 1
    
    paginator = Paginator(queryset, per_page)
    page_obj = paginator.get_page(page_num)
    
    return {
        'page_obj': page_obj,
        'current_page': page_obj.number,
        'total_pages': paginator.num_pages,
        'has_prev': page_obj.has_previous(),
        'has_next': page_obj.has_next(),
        'prev_page': page_obj.previous_page_number() if page_obj.has_previous() else None,
        'next_page': page_obj.next_page_number() if page_obj.has_next() else None,
    }
```

2. 替换以下视图中的分页代码：
   - `core/views/taxonomy.py` - taxonomy_list_view
   - `core/views/cron.py` - permission_check（已部分使用）
   - `core/views/users.py` - 用户列表视图

**涉及文件：**
- `core/utils/pagination.py`（新建）
- `core/views/taxonomy.py`
- `core/views/cron.py`
- `core/views/users.py`

**风险评估：** 低风险，纯工具函数提取。

---

### 优化 3：基础服务类 `BaseService`

**目标：** 统一服务层的公共方法，消除 `get_by_id` 等方法重复。

**当前问题：**
- `core/services/user_service.py:48-53`
- `core/services/taxonomy_service.py:366-369`
- `core/node/services/node_service.py:15-16`
- `modules/customer/services.py:83-85`

**实施方案：**

1. 创建 `core/services/base_service.py`：
```python
class BaseService:
    """服务基类，提供通用 CRUD 方法"""
    model_class = None
    
    @classmethod
    def get_by_id(cls, entity_id):
        if cls.model_class is None:
            raise NotImplementedError('子类必须定义 model_class')
        return cls.model_class.objects.filter(id=entity_id).first()
    
    @classmethod
    def get_list(cls, **filters):
        if cls.model_class is None:
            raise NotImplementedError('子类必须定义 model_class')
        return cls.model_class.objects.filter(**filters)
```

2. 改造现有服务类继承 `BaseService`：
```python
class UserService(BaseService):
    model_class = User
    # 其他方法保持不变
```

3. 渐进式替换，不强制一次性全部改造。

**涉及文件：**
- `core/services/base_service.py`（新建）
- `core/services/user_service.py`
- `core/services/taxonomy_service.py`
- `core/services/settings_service.py`
- `core/node/services/node_service.py`
- `modules/customer/services.py`

**风险评估：** 中等风险，需要确保所有现有调用兼容。建议渐进式迁移。

---

### 优化 4：表单控件样式统一

**目标：** 消除表单中重复的 Bootstrap class 定义。

**当前问题：** `form-control form-control-lg` 等 class 在 `core/forms/admin_forms.py` 中每个字段重复定义。

**实施方案：**

1. 创建 `core/forms/mixins.py`：
```python
from django import forms

class BootstrapFormMixin:
    """自动为表单字段添加 Bootstrap class"""
    large = True  # 是否使用 large 尺寸
    
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        size_class = 'form-control-lg' if self.large else ''
        select_size = 'form-select-lg' if self.large else ''
        
        for field in self.fields.values():
            if isinstance(field.widget, forms.TextInput):
                field.widget.attrs.update({'class': f'form-control {size_class}'.strip()})
            elif isinstance(field.widget, forms.Textarea):
                field.widget.attrs.update({'class': 'form-control'})
            elif isinstance(field.widget, forms.Select):
                field.widget.attrs.update({'class': f'form-select {select_size}'.strip()})
            elif isinstance(field.widget, forms.PasswordInput):
                field.widget.attrs.update({'class': f'form-control {size_class}'.strip()})
            elif isinstance(field.widget, forms.EmailInput):
                field.widget.attrs.update({'class': f'form-control {size_class}'.strip()})
```

2. 让现有表单继承此 Mixin，逐步删除手动定义的 widget class。

**涉及文件：**
- `core/forms/mixins.py`（新建）
- `core/forms/admin_forms.py`
- `core/forms/settings_forms.py`
- `core/forms/__init__.py`

**风险评估：** 低风险，纯样式层改动，不影响功能。

---

### 优化 5：视图表单使用统一

**目标：** 所有视图统一使用 Django 表单，消除 `request.POST.get()` 直接取值。

**当前问题：** `core/views/users.py` 中 `user_create`、`user_edit` 直接使用 `request.POST.get()`。

**实施方案：**

1. 确认 `core/forms/admin_forms.py` 中已有 `UserCreateForm`、`UserEditForm`。
2. 改造 `core/views/users.py` 中的创建/编辑视图使用表单。
3. 检查其他视图是否存在类似问题。

**涉及文件：**
- `core/views/users.py`
- `core/forms/admin_forms.py`

**风险评估：** 中等风险，需要测试表单验证逻辑是否完整覆盖原有手动验证。

---

### 优化 6：角色徽章模板组件

**目标：** 消除角色 badge 渲染的重复 HTML。

**当前问题：** 角色 badge 在 `system_users.html`、`profile.html` 等多处重复定义。

**实施方案：**

1. 创建 `core/templates/includes/role_badge.html`：
```html
{% if role == 'manager' %}
    <span class="badge bg-danger">{{ role_labels.manager|default("一类用户") }}</span>
{% elif role == 'leader' %}
    <span class="badge bg-warning text-dark">{{ role_labels.leader|default("二类用户") }}</span>
{% else %}
    <span class="badge bg-secondary">{{ role_labels.employee|default("三类用户") }}</span>
{% endif %}
```

2. 在模板中使用 `{% include "includes/role_badge.html" with role=user.role %}` 替换重复代码。

**涉及文件：**
- `core/templates/includes/role_badge.html`（新建）
- `core/templates/admin/system_users.html`
- `core/templates/usermenu/profile.html`

**风险评估：** 低风险，纯模板层改动。

---

## 三、执行顺序

| 步骤 | 优化项 | 预计耗时 | 依赖 |
|------|--------|----------|------|
| 1 | 抽象基础模型 BaseModel | 15 min | 无 |
| 2 | 分页工具函数 | 20 min | 无 |
| 3 | 基础服务类 BaseService | 30 min | 1、2 |
| 4 | 表单控件样式统一 | 20 min | 无 |
| 5 | 视图表单使用统一 | 25 min | 4 |
| 6 | 角色徽章模板组件 | 15 min | 无 |

**预计总耗时：** 约 2 小时

---

## 四、验收标准

1. 所有模型继承 `BaseModel`，无重复时间戳字段
2. 所有分页视图使用 `paginate_queryset()` 工具函数
3. 至少 3 个服务类继承 `BaseService`
4. 所有表单使用 `BootstrapFormMixin`
5. `users.py` 中的创建/编辑视图使用 Django 表单
6. 角色 badge 使用 include 组件，无重复 HTML

---

## 五、回滚方案

每个优化项独立执行，如出现问题可单独回滚：
- BaseModel：恢复各模型的时间戳字段定义
- 分页工具：恢复原有分页代码
- BaseService：恢复原有 staticmethod 调用
- 表单 Mixin：恢复手动 widget 配置
