# 居民信息模块实现指南

本文档详细介绍居民信息管理模块的设计与实现。

---

## 一、模块概述

### 1.1 基本信息

| 属性 | 值 |
|------|-----|
| 模块 ID | `resident_info` |
| 模块名称 | 居民信息 |
| 模块类型 | `node` |
| 描述 | 管理居民住户信息，适合居委会及网格员使用 |
| 图标 | `bi-person-vcard` |

### 1.2 数据来源

- 导入模板：`temp/person_导入模板.xlsx`
- 字段数量：29 个原始字段 + 1 个省市县联动字段 = 30 个

---

## 二、字段设计

### 2.1 字段总览

| # | 字段名 | Django 字段类型 | 词汇表 | 说明 |
|---|--------|---------------|--------|------|
| 1 | name | `CharField(max_length=100)` | - | 姓名 |
| 2 | relation | `ForeignKey` | `resident_relation` | 与其他人员关系 |
| 3 | id_card | `CharField(max_length=18)` | - | 身份证号 |
| 4 | gender | `ForeignKey` | `gender` | 性别（复用预置） |
| 5 | birth_date | `DateField` | - | 出生日期 |
| 6 | phone | `CharField(max_length=100)` | - | 联系电话 |
| 7 | current_community | `CharField(max_length=200)` | - | 现住小区/建筑 |
| 8 | current_door | `CharField(max_length=100)` | - | 门牌地址 |
| 9 | grid | `ForeignKey` | `grid` | 所属网格 |
| 10 | resident_type | `ForeignKey` | `resident_type` | 人员类型 |
| 11 | is_key_person | `BooleanField(default=False)` | - | 是否重点人员 |
| 12 | key_category | `ForeignKey` | `key_category` | 重点类别 |
| 13 | registered_community | `CharField(max_length=200)` | - | 户籍小区/建筑 |
| 14 | registered_address | `CharField(max_length=200)` | - | 户籍地址 |
| 15 | registered_region | `JSONField` | - | 户籍地址省市区（省市县联动） |
| 16 | household_number | `CharField(max_length=50)` | - | 户编号 |
| 17 | is_separated | `BooleanField(default=False)` | - | 是否人户分离 |
| 18 | actual_residence | `CharField(max_length=200)` | - | 实际居住地 |
| 19 | is_moved_out | `BooleanField(default=False)` | - | 是否已迁出 |
| 20 | move_out_date | `DateField(null=True, blank=True)` | - | 迁出日期 |
| 21 | move_to_place | `CharField(max_length=200)` | - | 迁往地 |
| 22 | is_deceased | `BooleanField(default=False)` | - | 是否已死亡 |
| 23 | death_date | `DateField(null=True, blank=True)` | - | 死亡日期 |
| 24 | nation | `ForeignKey` | `nation` | 民族 |
| 25 | political_status | `ForeignKey` | `political_status` | 政治面貌 |
| 26 | marital_status | `ForeignKey` | `marital_status` | 婚姻状况 |
| 27 | education | `ForeignKey` | `education` | 文化程度 |
| 28 | work_status | `CharField(max_length=50)` | - | 工作学习情况 |
| 29 | health_status | `ForeignKey` | `health_status` | 健康状况 |
| 30 | notes | `TextField(blank=True)` | - | 备注 |

### 2.2 字段分组

```
基础信息（1-6）
├── name         - 姓名
├── relation     - 与其他人员关系
├── id_card      - 身份证号
├── gender       - 性别
├── birth_date   - 出生日期
└── phone        - 联系电话

现住地址（7-9）
├── current_community - 现住小区/建筑
├── current_door     - 门牌地址
└── grid             - 所属网格

人员状态（10-12）
├── resident_type  - 人员类型
├── is_key_person  - 是否重点人员
└── key_category   - 重点类别

户籍信息（13-16）
├── registered_community  - 户籍小区/建筑
├── registered_address    - 户籍地址
├── registered_region    - 户籍地址省市区（省市县联动）
└── household_number     - 户编号

人户分离（17-18）
├── is_separated     - 是否人户分离
└── actual_residence - 实际居住地

迁出信息（19-21）
├── is_moved_out   - 是否已迁出
├── move_out_date  - 迁出日期
└── move_to_place  - 迁往地

死亡信息（22-23）
├── is_deceased  - 是否已死亡
└── death_date   - 死亡日期

个人信息（24-30）
├── nation           - 民族
├── political_status - 政治面貌
├── marital_status   - 婚姻状况
├── education        - 文化程度
├── work_status      - 工作学习情况
├── health_status    - 健康状况
└── notes            - 备注
```

---

## 三、词汇表设计

### 3.1 需要新建的词汇表

| 词汇表 slug | 名称 | 选项 |
|-------------|------|------|
| `resident_relation` | 与户主关系 | 户主、配偶、子女、父母、租户、其他 |
| `resident_type` | 人员类型 | 常住人口、流动人口、暂住人口 |
| `grid` | 所属网格 | （需根据实际情况配置） |
| `key_category` | 重点类别 | 独居老人、低保户、残疾人、重症患者、刑满释放人员、社区矫正对象、涉毒人员、其他 |
| `nation` | 民族 | 汉族、壮族、满族、回族、苗族、维吾尔族、蒙古族、藏族、布依族、彝族、侗族、瑶族、白族、土家族、哈尼族、傣族、黎族、傈僳族、佤族、畲族、高山族、拉祜族、水族、东乡族、纳西族、景颇族、柯尔克孜族、土族、达斡尔族、仫佬族、羌族、布朗族、撒拉族、毛南族、仡佬族、锡伯族、阿昌族、普米族、塔吉克族、怒族、乌孜别克族、俄罗斯族、鄂温克族、德昂族、保安族、裕固族、京族、塔塔尔族、独龙族、鄂伦春族、赫哲族、门巴族、珞巴族、基诺族、其他 |
| `political_status` | 政治面貌 | 中共党员、中共预备党员、共青团员、民革党员、民盟盟员、民建会员、民进会员、农工党党员、致公党党员、九三学社社员、台盟盟员、无党派人士、群众 |
| `marital_status` | 婚姻状况 | 未婚、已婚、离异、丧偶 |
| `education` | 文化程度 | 研究生、大学本科、大学专科、中专、普通高中、初中、小学、文盲 |
| `health_status` | 健康状况 | 健康、良好、残疾、疾病、慢性病 |

### 3.2 复用的预置词汇表

| 词汇表 slug | 来源 |
|-------------|------|
| `gender` | 预置词汇表 |

---

## 四、模型设计

### 4.1 ResidentInfoFields 模型

```python
# modules/resident_info/models.py
# -*- coding: utf-8 -*-
"""
居民信息模型
"""

from django.db import models
from core.node.models import Node


class ResidentInfoFields(models.Model):
    """居民信息字段表"""
    
    node = models.OneToOneField(
        Node,
        on_delete=models.CASCADE,
        related_name='resident_info_fields',
        verbose_name='关联节点'
    )
    
    # ========== 基础信息 ==========
    name = models.CharField(max_length=100, verbose_name='姓名')
    relation = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='resident_relation_residents',
        verbose_name='与其他人员关系'
    )
    id_card = models.CharField(max_length=18, verbose_name='身份证号')
    gender = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='gender_residents',
        verbose_name='性别'
    )
    birth_date = models.DateField(null=True, blank=True, verbose_name='出生日期')
    phone = models.CharField(max_length=100, blank=True, verbose_name='联系电话')
    
    # ========== 现住地址 ==========
    current_community = models.CharField(max_length=200, blank=True, verbose_name='现住小区/建筑')
    current_door = models.CharField(max_length=100, blank=True, verbose_name='门牌地址')
    grid = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='grid_residents',
        verbose_name='所属网格'
    )
    
    # ========== 人员状态 ==========
    resident_type = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='resident_type_residents',
        verbose_name='人员类型'
    )
    is_key_person = models.BooleanField(default=False, verbose_name='是否重点人员')
    key_category = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='key_category_residents',
        verbose_name='重点类别'
    )
    
    # ========== 户籍信息 ==========
    registered_community = models.CharField(max_length=200, blank=True, verbose_name='户籍小区/建筑')
    registered_address = models.CharField(max_length=200, blank=True, verbose_name='户籍地址')
    registered_region = models.JSONField(blank=True, null=True, verbose_name='户籍地址省市区')
    household_number = models.CharField(max_length=50, blank=True, verbose_name='户编号')
    
    # ========== 人户分离 ==========
    is_separated = models.BooleanField(default=False, verbose_name='是否人户分离')
    actual_residence = models.CharField(max_length=200, blank=True, verbose_name='实际居住地')
    
    # ========== 迁出信息 ==========
    is_moved_out = models.BooleanField(default=False, verbose_name='是否已迁出')
    move_out_date = models.DateField(null=True, blank=True, verbose_name='迁出日期')
    move_to_place = models.CharField(max_length=200, blank=True, verbose_name='迁往地')
    
    # ========== 死亡信息 ==========
    is_deceased = models.BooleanField(default=False, verbose_name='是否已死亡')
    death_date = models.DateField(null=True, blank=True, verbose_name='死亡日期')
    
    # ========== 个人信息 ==========
    nation = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='nation_residents',
        verbose_name='民族'
    )
    political_status = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='political_status_residents',
        verbose_name='政治面貌'
    )
    marital_status = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='marital_status_residents',
        verbose_name='婚姻状况'
    )
    education = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='education_residents',
        verbose_name='文化程度'
    )
    work_status = models.CharField(max_length=50, blank=True, verbose_name='工作学习情况')
    health_status = models.ForeignKey(
        'core.TaxonomyItem',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='health_status_residents',
        verbose_name='健康状况'
    )
    notes = models.TextField(blank=True, verbose_name='备注')
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        db_table = 'resident_info_fields'
        verbose_name = '居民'
        verbose_name_plural = '居民'
    
    @property
    def creator(self):
        return self.node.created_by if hasattr(self, 'node') and self.node else None
    
    def __str__(self):
        return self.name
```

---

## 五、服务层设计

### 5.1 ResidentInfoService

```python
# modules/resident_info/services.py
# -*- coding: utf-8 -*-
"""
居民信息服务
"""

from typing import List, Optional, Dict, Any
from django.contrib.auth import get_user_model
from django.db.models import Q
from .models import ResidentInfoFields
from core.node.services import NodeService

User = get_user_model()


class ResidentInfoService:
    """居民信息管理服务"""
    
    @staticmethod
    def get_list(search: Optional[str] = None, resident_type_id: Optional[int] = None,
                grid_id: Optional[int] = None, user=None) -> List[ResidentInfoFields]:
        """获取居民列表
        
        Args:
            search: 搜索关键词（姓名、身份证号、电话）
            resident_type_id: 人员类型ID
            grid_id: 网格ID
            user: 当前用户
        """
        queryset = ResidentInfoFields.objects.select_related(
            'node', 'relation', 'gender', 'grid', 'resident_type', 
            'key_category', 'nation', 'political_status', 
            'marital_status', 'education', 'health_status'
        )
        
        # 权限过滤
        if user and not user.is_admin:
            queryset = queryset.filter(node__created_by=user)
        
        if search:
            queryset = queryset.filter(
                Q(name__icontains=search) |
                Q(id_card__icontains=search) |
                Q(phone__icontains=search)
            )
        
        if resident_type_id:
            queryset = queryset.filter(resident_type_id=resident_type_id)
        
        if grid_id:
            queryset = queryset.filter(grid_id=grid_id)
        
        return queryset.order_by('-created_at')
    
    @staticmethod
    def get_by_id(resident_id: int) -> Optional[ResidentInfoFields]:
        """根据 ID 获取居民"""
        return ResidentInfoFields.objects.filter(id=resident_id).first()
    
    @staticmethod
    def get_by_node_id(node_id: int) -> Optional[ResidentInfoFields]:
        """根据节点 ID 获取居民"""
        return ResidentInfoFields.objects.filter(node_id=node_id).first()
    
    @staticmethod
    def create(user, data: Dict[str, Any]) -> ResidentInfoFields:
        """创建居民信息"""
        node = NodeService.create('resident_info', user, {})
        
        resident = ResidentInfoFields.objects.create(
            node=node,
            name=data.get('name', ''),
            relation_id=data.get('relation_id'),
            id_card=data.get('id_card'),
            gender_id=data.get('gender_id'),
            birth_date=data.get('birth_date'),
            phone=data.get('phone'),
            current_community=data.get('current_community'),
            current_door=data.get('current_door'),
            grid_id=data.get('grid_id'),
            resident_type_id=data.get('resident_type_id'),
            is_key_person=data.get('is_key_person', False),
            key_category_id=data.get('key_category_id'),
            registered_community=data.get('registered_community'),
            registered_address=data.get('registered_address'),
            registered_region=data.get('registered_region'),
            household_number=data.get('household_number'),
            is_separated=data.get('is_separated', False),
            actual_residence=data.get('actual_residence'),
            is_moved_out=data.get('is_moved_out', False),
            move_out_date=data.get('move_out_date'),
            move_to_place=data.get('move_to_place'),
            is_deceased=data.get('is_deceased', False),
            death_date=data.get('death_date'),
            nation_id=data.get('nation_id'),
            political_status_id=data.get('political_status_id'),
            marital_status_id=data.get('marital_status_id'),
            education_id=data.get('education_id'),
            work_status=data.get('work_status'),
            health_status_id=data.get('health_status_id'),
            notes=data.get('notes'),
        )
        
        return resident
    
    @staticmethod
    def update(resident_id: int, user, data: Dict[str, Any]) -> Optional[ResidentInfoFields]:
        """更新居民信息"""
        resident = ResidentInfoFields.objects.filter(id=resident_id).first()
        if not resident:
            return None
        
        NodeService.update(resident.node_id, user, {})
        
        update_fields = [
            'name', 'relation_id', 'id_card', 'gender_id', 'birth_date', 'phone',
            'current_community', 'current_door', 'grid_id',
            'resident_type_id', 'is_key_person', 'key_category_id',
            'registered_community', 'registered_address', 'registered_region', 'household_number',
            'is_separated', 'actual_residence',
            'is_moved_out', 'move_out_date', 'move_to_place',
            'is_deceased', 'death_date',
            'nation_id', 'political_status_id', 'marital_status_id', 'education_id', 
            'work_status', 'health_status_id', 'notes'
        ]
        
        for field in update_fields:
            if field in data:
                setattr(resident, field, data[field])
        
        resident.save()
        return resident
    
    @staticmethod
    def delete(resident_id: int) -> bool:
        """删除居民信息"""
        resident = ResidentInfoFields.objects.filter(id=resident_id).first()
        if resident:
            node = resident.node
            resident.delete()
            if node:
                node.delete()
            return True
        return False
    
    @staticmethod
    def get_exportable_fields() -> List[Dict]:
        """获取可导出的字段列表"""
        return [
            {'name': 'name', 'label': '姓名', 'type': 'string', 'required': True},
            {'name': 'relation', 'label': '与其他人员关系', 'type': 'fk'},
            {'name': 'id_card', 'label': '身份证号', 'type': 'string', 'required': True},
            {'name': 'gender', 'label': '性别', 'type': 'fk'},
            {'name': 'birth_date', 'label': '出生日期', 'type': 'date'},
            {'name': 'phone', 'label': '联系电话', 'type': 'telephone'},
            {'name': 'current_community', 'label': '现住小区/建筑', 'type': 'string'},
            {'name': 'current_door', 'label': '门牌地址', 'type': 'string'},
            {'name': 'grid', 'label': '所属网格', 'type': 'fk'},
            {'name': 'resident_type', 'label': '人员类型', 'type': 'fk'},
            {'name': 'is_key_person', 'label': '是否重点人员', 'type': 'boolean'},
            {'name': 'key_category', 'label': '重点类别', 'type': 'fk'},
            {'name': 'registered_community', 'label': '户籍小区/建筑', 'type': 'string'},
            {'name': 'registered_address', 'label': '户籍地址', 'type': 'string'},
            {'name': 'registered_region', 'label': '户籍地址省市区', 'type': 'region'},
            {'name': 'household_number', 'label': '户编号', 'type': 'string'},
            {'name': 'is_separated', 'label': '是否人户分离', 'type': 'boolean'},
            {'name': 'actual_residence', 'label': '实际居住地', 'type': 'string'},
            {'name': 'is_moved_out', 'label': '是否已迁出', 'type': 'boolean'},
            {'name': 'move_out_date', 'label': '迁出日期', 'type': 'date'},
            {'name': 'move_to_place', 'label': '迁往地', 'type': 'string'},
            {'name': 'is_deceased', 'label': '是否已死亡', 'type': 'boolean'},
            {'name': 'death_date', 'label': '死亡日期', 'type': 'date'},
            {'name': 'nation', 'label': '民族', 'type': 'fk'},
            {'name': 'political_status', 'label': '政治面貌', 'type': 'fk'},
            {'name': 'marital_status', 'label': '婚姻状况', 'type': 'fk'},
            {'name': 'education', 'label': '文化程度', 'type': 'fk'},
            {'name': 'work_status', 'label': '工作学习情况', 'type': 'string'},
            {'name': 'health_status', 'label': '健康状况', 'type': 'fk'},
            {'name': 'notes', 'label': '备注', 'type': 'text'},
        ]
    
    @staticmethod
    def get_count() -> int:
        """获取居民总数"""
        return ResidentInfoFields.objects.count()
    
    @staticmethod
    def get_recent_count(days: int = 7) -> int:
        """获取最近N天新增的居民数量"""
        from django.utils import timezone
        from datetime import timedelta
        start_date = timezone.now() - timedelta(days=days)
        return ResidentInfoFields.objects.filter(created_at__gte=start_date).count()
```

---

## 六、模块配置

### 6.1 module.py

```python
# modules/resident_info/module.py
# -*- coding: utf-8 -*-
"""
模块信息
"""

MODULE_INFO = {
    'id': 'resident_info',
    'name': '居民信息',
    'type': 'node',
    'version': '1.0.0',
    'author': '',
    'description': '管理居民住户信息，适合居委会及网格员使用。',
    'icon': 'bi-person-vcard',
    'dashboard_cards': [
        {
            'id': 'resident_info_card',
            'name': '居民信息',
            'template': 'resident_info/dashboard_card.html',
        }
    ],
    'taxonomies': [
        {'slug': 'resident_relation', 'name': '与户主关系', 'items': ['户主', '配偶', '子女', '父母', '租户', '其他']},
        {'slug': 'resident_type', 'name': '人员类型', 'items': ['常住人口', '流动人口', '暂住人口']},
        {'slug': 'grid', 'name': '所属网格', 'items': ['网格1', '网格2', '网格3', '网格4', '网格5']},
        {'slug': 'key_category', 'name': '重点类别', 'items': ['独居老人', '低保户', '残疾人', '重症患者', '刑满释放人员', '社区矫正对象', '涉毒人员', '其他']},
        {'slug': 'nation', 'name': '民族', 'items': ['汉族', '壮族', '满族', '回族', '苗族', '维吾尔族', '蒙古族', '藏族', '其他']},
        {'slug': 'political_status', 'name': '政治面貌', 'items': ['中共党员', '中共预备党员', '共青团员', '群众']},
        {'slug': 'marital_status', 'name': '婚姻状况', 'items': ['未婚', '已婚', '离异', '丧偶']},
        {'slug': 'education', 'name': '文化程度', 'items': ['研究生', '本科', '大专', '高中', '初中', '小学', '文盲']},
        {'slug': 'health_status', 'name': '健康状况', 'items': ['健康', '良好', '残疾', '疾病', '慢性病']},
    ],
}
```

### 6.2 权限配置

居民信息模块的权限由**系统自动生成**，无需手动配置。

系统会自动生成以下 7 个权限：

| 权限Key | 说明 | 适用范围 |
|--------|------|----------|
| `node.resident_info.create` | 创建居民 | 所有人 |
| `node.resident_info.view` | 查看自己的数据 | 创建者本人 |
| `node.resident_info.edit` | 编辑自己的数据 | 创建者本人 |
| `node.resident_info.delete` | 删除自己的数据 | 创建者本人 |
| `node.resident_info.view_others` | 查看别人的数据 | 需额外授权 |
| `node.resident_info.edit_others` | 编辑别人的数据 | 需额外授权 |
| `node.resident_info.delete_others` | 删除别人的数据 | 需额外授权 |

### 6.3 权限调用示例

在 views.py 中调用权限检查：

```python
# modules/resident_info/views.py
from core.services import PermissionService

def check_resident_permission(user, node, permission_type: str):
    """检查居民信息操作权限"""
    if user.is_admin:
        return True, None
    
    is_creator = node.created_by_id == user.id
    if is_creator:
        return True, None
    
    perm_map = {
        'view': 'node.resident_info.view_others',
        'edit': 'node.resident_info.edit_others',
        'delete': 'node.resident_info.delete_others',
    }
    
    perm = perm_map.get(permission_type)
    if perm and PermissionService.has_permission(user, perm):
        return True, None
    
    return False, f'您没有权限{permission_type}别人的居民信息'
```

---

## 七、导入功能对接

### 7.1 字段映射

| Excel 列名 | 模型字段 | 类型转换 |
|------------|---------|---------|
| 姓名 | name | string |
| 与其他人员关系 | relation | FK by name |
| 身份证号 | id_card | string |
| 性别 | gender | FK by name |
| 出生日期 | birth_date | date (YYYYMMDD) |
| 联系电话 | phone | string |
| 现住小区/建筑 | current_community | string |
| 门牌地址 | current_door | string |
| 所属网格 | grid | FK by name |
| 人员类型 | resident_type | FK by name |
| 是否重点人员 | is_key_person | boolean (1/0) |
| 重点类别 | key_category | FK by name (多选用,分隔) |
| 户籍小区/建筑 | registered_community | string |
| 户籍地址 | registered_address | string |
| 户编号 | household_number | string |
| 是否人户分离 | is_separated | boolean (1/0) |
| 实际居住地 | actual_residence | string |
| 是否已迁出 | is_moved_out | boolean (1/0) |
| 迁出日期 | move_out_date | date (YYYYMMDD) |
| 迁往地 | move_to_place | string |
| 是否已死亡 | is_deceased | boolean (1/0) |
| 死亡日期 | death_date | date (YYYYMMDD) |
| 民族 | nation | FK by name |
| 政治面貌 | political_status | FK by name |
| 婚姻状况 | marital_status | FK by name |
| 文化程度 | education | FK by name |
| 工作学习情况 | work_status | string |
| 健康状况 | health_status | FK by name |
| 备注 | notes | text |

### 7.2 特殊处理

1. **日期格式转换**：Excel 中的日期格式为 `YYYYMMDD`，需要转换为 Python `date` 对象
2. **布尔值转换**：Excel 中使用 `1`/`0` 表示布尔值
3. **多选字段**：`重点类别` 字段使用 `,` 分隔多个值，需转换为多对多关系或存储为 JSON

---

## 八、开发步骤

### 步骤 1：创建模型并迁移

```bash
./venv/bin/python manage.py makemigrations resident_info
./venv/bin/python manage.py migrate
```

### 步骤 2：扫描并安装模块

在管理页面执行：扫描模块 → 安装 → 启用

### 步骤 3：创建词汇表

在系统中创建以下词汇表：
- resident_relation
- resident_type
- grid
- key_category
- nation
- political_status
- marital_status
- education
- health_status

### 步骤 4：创建模板

创建以下模板文件：
- `modules/resident_info/templates/resident_info/list.html`
- `modules/resident_info/templates/resident_info/edit.html`
- `modules/resident_info/templates/resident_info/view.html`
- `modules/resident_info/templates/resident_info/dashboard_card.html`

### 步骤 5：对接导入功能

在 `core/importexport` 中注册该模块的导入字段映射

---

## 九、参考文档

- [02_模块技术规范](../技术规范/02_模块技术规范.md)
- [14_客户信息Node模块实现指南](./14_客户信息Node模块实现指南.md)
- [17_客户信息国内模块](./17_客户信息国内模块.md)
- [03_省市县联动字段技术规范](../技术规范/03_省市县联动字段技术规范.md)

---

*文档版本：1.1*  
*最后更新：2026-03-29*
