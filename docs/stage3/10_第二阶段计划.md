# 第二阶段：功能完善计划

## 阶段概述

在完成基础迁移后，第二阶段聚焦于功能完善，补充缺失的业务模块、表单系统、静态资源和辅助功能。

---

## 一、当前实现状态

### 已完成 ✅
| 模块 | 内容 |
|------|------|
| 核心模型 | User, SystemSetting, Taxonomy, TaxonomyItem |
| 节点模型 | NodeType, Node, CustomerFields |
| 核心服务 | SettingsService, PermissionService, UserService, AuthService, WatermarkService |
| 节点服务 | NodeTypeService, NodeService, CustomerService |
| 视图路由 | 登录、仪表盘、用户管理、系统设置、词汇表、客户CRUD |
| 模板 | 基础模板 + 业务模板 (21个) |
| 启动脚本 | run.py, run.sh, manage.py |

### 待实现 ❌
| 类别 | 内容 |
|------|------|
| 表单系统 | auth_forms, admin_forms, settings_forms, 节点表单 |
| 个人中心 | profile, settings |
| 导出功能 | export service + 视图 |
| 时间服务 | time_service, time_sync_service |
| Cron任务 | cron_service + management command |
| 字段系统 | 24种字段类型 |
| 静态资源 | CSS主题, JS工具库 |
| 初始化脚本 | init_db.py |

---

## 二、详细实施计划

### 阶段A：表单系统 (Form System)

**目标**：建立 Django Forms 体系，替代 Flask WTForms

**文件清单**：
```
core/forms/
├── __init__.py
├── auth_forms.py       # 登录、注册表单
├── admin_forms.py      # 用户管理表单
└── settings_forms.py  # 系统设置表单

nodes/forms.py          # 节点通用表单
```

**任务内容**：
1. 创建 core/forms/__init__.py
2. 迁移 auth_forms.py - LoginForm
3. 迁移 admin_forms.py - UserCreateForm, UserEditForm
4. 迁移 settings_forms.py - SystemSettingsForm
5. 创建节点通用表单基类
6. 创建客户表单

---

### 阶段B：个人中心 (Profile)

**目标**：实现用户个人资料管理和偏好设置

**文件清单**：
```
core/views.py          # 添加 profile 相关视图
core/urls.py          # 添加 profile 路由

templates/core/
├── profile.html       # 个人资料页面
└── settings.html     # 个人设置页面
```

**任务内容**：
1. 添加 profile 路由 (/profile/, /profile/edit/, /profile/settings/)
2. 实现 profile_view 视图
3. 实现 profile_edit 视图
4. 实现 profile_settings 视图
5. 创建 profile.html 模板
6. 创建 settings.html 模板

---

### 阶段C：字段系统 (Field Types)

**目标**：实现 24 种字段类型，支持节点动态字段配置

**文件清单**：
```
core/fields/
├── __init__.py
├── base.py            # 字段基类 FieldType
├── string.py          # 字符串
├── string_long.py     # 长文本
├── text.py            # 富文本
├── text_long.py       # 长富文本
├── integer.py         # 整数
├── float.py           # 浮点数
├── decimal.py         # 小数
├── boolean.py         # 布尔值
├── email.py           # 邮箱
├── telephone.py       # 电话
├── masked.py          # 掩码
├── image.py           # 图片
├── file.py            # 文件
├── datetime.py        # 日期时间
├── timestamp.py       # 时间戳
├── address.py         # 地址
├── geolocation.py     # 地理位置
├── link.py            # 链接
├── entity_reference.py # 实体引用
├── color.py           # 颜色
├── identity.py        # 身份标识
├── biometric.py       # 生物识别
└── ai_tags.py        # AI标签
```

**任务内容**：
1. 创建字段基类 FieldType
2. 实现基础字段类型 (string, integer, boolean, email 等)
3. 实现文件字段类型 (image, file)
4. 实现复杂字段类型 (address, geolocation, entity_reference)
5. 实现特殊字段类型 (color, biometric, ai_tags)
6. 创建字段注册机制

---

### 阶段D：导出功能 (Export)

**目标**：实现数据导出为 CSV、Excel、PDF

**文件清单**：
```
core/services/
└── export_service.py  # 导出服务

core/views.py          # 添加 export 视图
core/urls.py          # 添加 export 路由

templates/core/
└── export.html        # 导出页面
```

**任务内容**：
1. 创建 ExportService 类
2. 实现 CSV 导出方法
3. 实现 Excel 导出方法 (可选)
4. 实现 PDF 导出方法
5. 添加导出视图
6. 创建导出页面模板

---

### 阶段E：时间同步服务 (Time Sync)

**目标**：实现系统时间同步功能

**文件清单**：
```
core/services/
├── time_service.py        # 时间服务
└── time_sync_service.py  # 时间同步服务

core/views.py              # 添加 time 视图
core/urls.py              # 添加 time 路由
```

**任务内容**：
1. 创建 TimeService 类
2. 创建 TimeSyncService 类
3. 实现时间同步逻辑
4. 添加时间相关视图 (获取当前时间、同步时间)

---

### 阶段F：Cron 定时任务 (Cron Jobs)

**目标**：实现定时任务调度系统

**文件清单**：
```
core/services/
└── cron_service.py      # Cron 服务

core/management/
└── commands/
    ├── __init__.py
    └── run_cron.py     # Django management command

core/views.py            # 添加 cron 视图
core/urls.py            # 添加 cron 路由

templates/core/admin/
└── cron.html           # Cron 管理页面
```

**任务内容**：
1. 创建 CronService 类
2. 定义任务基类和具体任务
3. 创建 Django management command
4. 添加 cron 管理视图
5. 创建 cron 管理页面模板

---

### 阶段G：静态资源 (Static Files)

**目标**：补充 CSS 和 JavaScript 资源

**文件清单**：
```
static/
├── css/
│   ├── base.css        # 基础样式
│   ├── frame.css       # 框架样式
│   ├── custom.css      # 自定义样式
│   ├── watermark.css   # 水印样式
│   └── themes/
│       ├── default.css
│       ├── dopamine.css
│       ├── macaron.css
│       ├── teal.css
│       ├── uniklo.css
│       └── gov.css
└── js/
    ├── common.js       # 通用函数库
    ├── main.js         # 主脚本
    └── watermark.js    # ✅ 已存在
```

**任务内容**：
1. 迁移 base.css
2. 迁移 frame.css
3. 迁移 custom.css
4. 迁移 watermark.css
5. 迁移 6 个主题 CSS
6. 迁移 common.js
7. 迁移 main.js

---

### 阶段H：初始化脚本 (Initialization)

**目标**：创建数据库初始化脚本

**文件清单**：
```
init_db.py              # 数据库初始化脚本
```

**任务内容**：
1. 创建 init_db.py
2. 实现数据库表创建
3. 实现默认管理员创建
4. 实现默认数据初始化

---

## 三、时间估算

| 阶段 | 内容 | 预估时间 |
|------|------|----------|
| A | 表单系统 | 1天 |
| B | 个人中心 | 1天 |
| C | 字段系统 | 2天 |
| D | 导出功能 | 1天 |
| E | 时间同步服务 | 1天 |
| F | Cron 定时任务 | 1天 |
| G | 静态资源 | 1天 |
| H | 初始化脚本 | 0.5天 |
| **合计** | | **8.5天** |

---

## 四、实施顺序建议

### 顺序一：快速启动优先
1. **G** → 静态资源 (立即可用)
2. **H** → 初始化脚本 (可运行项目)
3. **A** → 表单系统 (完善表单验证)
4. **B** → 个人中心 (基础功能)
5. **D** → 导出功能 (业务需求)
6. **E** → 时间服务 (辅助功能)
7. **F** → Cron任务 (高级功能)
8. **C** → 字段系统 (扩展使用)

### 顺序二：依赖关系优先
1. **H** → 初始化脚本 (基础)
2. **G** → 静态资源 (UI基础)
3. **A** → 表单系统 (表单验证)
4. **B** → 个人中心 (用户功能)
5. **D** → 导出功能 (业务功能)
6. **E** → 时间服务 (依赖H)
7. **F** → Cron任务 (依赖E)
8. **C** → 字段系统 (最后实现)

---

## 五、注意事项

1. **参考旧版本**：每个阶段的实现都必须参考 `/home/edo/cimf-flask/` 中的对应模块
2. **保持一致性**：界面和功能需与 Flask 版本保持一致
3. **测试驱动**：每个阶段完成后进行基本测试
4. **文档更新**：每个阶段完成后更新 progress.md

---

## 六、阶段A 详细计划：表单系统

### A.1 任务清单

| 序号 | 文件 | 任务描述 | 参考来源 |
|------|------|----------|----------|
| A.1.1 | core/forms/__init__.py | 创建表单模块，导出所有表单类 | 新建 |
| A.1.2 | core/forms/auth_forms.py | 迁移 LoginForm | Flask auth_forms.py |
| A.1.3 | core/forms/admin_forms.py | 迁移 UserCreateForm, UserEditForm | Flask admin_forms.py |
| A.1.4 | core/forms/settings_forms.py | 迁移 SystemSettingsForm | Flask settings_forms.py |
| A.1.5 | nodes/forms.py | 创建节点通用表单 | 新建 |
| A.1.6 | nodes/customer/forms.py | 迁移 CustomerForm | Flask customer/forms.py |
| A.1.7 | core/views.py | 更新视图使用表单 | 修改现有 |
| A.1.8 | templates/ | 更新模板使用表单渲染 | 修改现有 |

### A.2 表单字段对应关系

#### A.2.1 LoginForm (auth_forms.py)
| 字段 | 类型 | 说明 |
|------|------|------|
| username | CharField | 用户名 |
| password | PasswordField | 密码 |
| remember_me | BooleanField | 记住我 |

#### A.2.2 UserCreateForm (admin_forms.py)
| 字段 | 类型 | 说明 |
|------|------|------|
| username | CharField | 用户名 |
| nickname | CharField | 昵称 |
| email | EmailField | 邮箱 |
| password | PasswordField | 密码 |
| role | ChoiceField | 角色 |
| is_admin | BooleanField | 管理员 |

#### A.2.3 UserEditForm (admin_forms.py)
| 字段 | 类型 | 说明 |
|------|------|------|
| username | CharField | 用户名 |
| nickname | CharField | 昵称 |
| email | EmailField | 邮箱 |
| password | PasswordField | 新密码(可选) |
| role | ChoiceField | 角色 |
| is_admin | BooleanField | 管理员 |
| is_active | BooleanField | 启用状态 |

#### A.2.4 SystemSettingsForm (settings_forms.py)
| 字段 | 类型 | 说明 |
|------|------|------|
| system_name | CharField | 系统名称 |
| upload_max_size_mb | IntegerField | 最大上传大小 |
| session_timeout_minutes | IntegerField | 会话超时 |
| enable_web_watermark | BooleanField | 启用网页水印 |
| enable_export_watermark | BooleanField | 启用导出水印 |
| time_zone | ChoiceField | 时区 |

### A.3 实施步骤

1. 创建 core/forms/ 目录
2. 创建 core/forms/__init__.py
3. 复制并修改 Flask 表单代码为 Django Forms
4. 创建 nodes/forms.py
5. 更新 core/views.py 中的视图使用表单
6. 更新模板使用 {{ form.as_p }} 或手动渲染

---

## 七、阶段B 详细计划：个人中心

### B.1 任务清单

| 序号 | 文件 | 任务描述 | 参考来源 |
|------|------|----------|----------|
| B.1.1 | core/urls.py | 添加 profile 路由 | Flask profile routes |
| B.1.2 | core/views.py | 添加 profile 视图函数 | Flask profile routes |
| B.1.3 | templates/core/profile.html | 个人资料模板 | Flask profile.html |
| B.1.4 | templates/core/settings.html | 个人设置模板 | Flask settings.html |

### B.2 路由设计

| 路由 | 视图函数 | 说明 |
|------|----------|------|
| /profile/ | profile_view | 个人资料查看 |
| /profile/edit/ | profile_edit | 个人资料编辑 |
| /profile/settings/ | profile_settings | 个人偏好设置 |

### B.3 功能说明

- profile_view: 显示用户信息 (用户名、昵称、邮箱、角色、最后登录)
- profile_edit: 编辑个人资料 (昵称、邮箱)
- profile_settings: 偏好设置 (主题、语言、通知开关)

---

## 八、阶段C 详细计划：字段系统

### C.1 字段类型清单

| 序号 | 文件 | 字段类型 | 说明 |
|------|------|----------|------|
| C.1.1 | base.py | FieldType | 所有字段的基类 |
| C.1.2 | string.py | StringField | 单行文本 |
| C.1.3 | string_long.py | StringLongField | 多行文本(无格式) |
| C.1.4 | text.py | TextField | 富文本 |
| C.1.5 | text_long.py | TextLongField | 长富文本 |
| C.1.6 | integer.py | IntegerField | 整数 |
| C.1.7 | float.py | FloatField | 浮点数 |
| C.1.8 | decimal.py | DecimalField | 小数 |
| C.1.9 | boolean.py | BooleanField | 布尔值 |
| C.1.10 | email.py | EmailField | 邮箱 |
| C.1.11 | telephone.py | TelephoneField | 电话号码 |
| C.1.12 | masked.py | MaskedField | 掩码(部分隐藏) |
| C.1.13 | image.py | ImageField | 图片上传 |
| C.1.14 | file.py | FileField | 文件上传 |
| C.1.15 | datetime.py | DateTimeField | 日期时间 |
| C.1.16 | timestamp.py | TimestampField | 时间戳 |
| C.1.17 | address.py | AddressField | 地址(复合) |
| C.1.18 | geolocation.py | GeolocationField | 地理坐标 |
| C.1.19 | link.py | LinkField | 网址链接 |
| C.1.20 | entity_reference.py | EntityReferenceField | 关联其他节点 |
| C.1.21 | color.py | ColorField | 颜色选择 |
| C.1.22 | identity.py | IdentityField | 身份标识 |
| C.1.23 | biometric.py | BiometricField | 生物识别数据 |
| C.1.24 | ai_tags.py | AITagsField | AI自动标签 |

### C.2 FieldType 基类接口

```python
class FieldType:
    name: str           # 字段名称
    field_type: str    # 字段类型标识
    widget: Widget      # 前端组件
    
    def get_form_field(self): pass     # 获取表单字段
    def get_db_field(self): pass       # 获取数据库字段
    def validate(self, value): pass    # 验证值
    def serialize(self, value): pass   # 序列化
    def deserialize(self, value): pass # 反序列化
    def render(self, value): pass      # 渲染HTML
```

---

## 九、阶段D 详细计划：导出功能

### D.1 导出格式

| 格式 | 方法 | 说明 |
|------|------|------|
| CSV | export_to_csv() | 通用导出 |
| Excel | export_to_excel() | 需要 openpyxl |
| PDF | export_to_pdf() | 需要 reportlab |

### D.2 导出服务接口

```python
class ExportService:
    @staticmethod
    def export_csv(queryset, fields, filename): pass
    
    @staticmethod
    def export_excel(queryset, fields, filename): pass
    
    @staticmethod
    def export_pdf(queryset, fields, filename, title): pass
```

---

## 十、阶段E 详细计划：时间同步

### E.1 服务接口

```python
class TimeService:
    @staticmethod
    def get_current_time(): pass
    
    @staticmethod
    def get_timezone(): pass
    
class TimeSyncService:
    @staticmethod
    def sync_from_server(url): pass
    
    @staticmethod
    def sync_periodically(interval): pass
```

---

## 十一、阶段F 详细计划：Cron任务

### F.1 任务清单

| 任务 | 说明 | 间隔 |
|------|------|------|
| time_sync | 时间同步 | 15分钟 |
| cache_cleanup | 缓存清理 | 3小时 |

### F.2 Management Command

```bash
python manage.py run_cron          # 执行所有任务
python manage.py run_cron --task=time_sync  # 执行指定任务
```

---

## 十二、阶段G 详细计划：静态资源

### G.1 CSS 文件

| 文件 | 说明 | 大小 |
|------|------|------|
| base.css | 基础重置和通用样式 | ~200行 |
| frame.css | 页面框架样式 | ~150行 |
| custom.css | 项目自定义覆盖 | ~100行 |
| watermark.css | 水印样式 | ~50行 |
| themes/*.css | 6个主题 | 各~100行 |

### G.2 JS 文件

| 文件 | 说明 | 大小 |
|------|------|------|
| common.js | 通用工具函数 | ~150行 |
| main.js | 主应用脚本 | ~100行 |
| watermark.js | ✅ 已存在 | - |

---

## 十三、阶段H 详细计划：初始化脚本

### H.1 init_db.py 功能

1. 检查数据库是否存在
2. 提示用户选择操作 (新建/备份重建)
3. 执行 migrate
4. 创建默认管理员账号 (admin/admin123456)
5. 初始化默认节点类型 (customer)
6. 初始化默认系统设置

---

**是否确认执行此计划？确认后开始按阶段实施。**
