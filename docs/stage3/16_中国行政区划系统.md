# 中国行政区划系统

## 需求概述

构建一个独立的中国行政区划管理系统，提供省-市-县三级联动数据支持，可被其他业务模块调用。

### 目标

1. **独立数据库表** - 不使用词汇表，创建专门的行政区划表
2. **后端服务** - 提供数据导入、层级关系计算、信息查询服务
3. **前端表单组件** - 提供三级联动选择和列表筛选功能
4. **测试页面** - 独立测试页面，不影响现有业务模块

---

## 数据量估算

| 层级 | 数量 | 说明 |
|------|------|------|
| 省级 | 34 | 23个省 + 5个自治区 + 4个直辖市 + 2个特别行政区 |
| 地级市/地区 | ~333 | 地级市、地区、自治州、盟 |
| 县级 | ~2800 | 市辖区、县级市、县、自治县、特区、林区 |
| **合计** | **~3200** | |

---

## 实施计划

### 步骤1：创建数据库模型

**文件**：`core/models.py`

```python
class ChinaRegion(models.Model):
    """
    中国行政区划
    
    使用自关联实现省-市-县三级层级结构
    """
    
    code = models.CharField(
        max_length=6,
        unique=True,
        verbose_name='行政区划代码'
    )
    
    name = models.CharField(
        max_length=100,
        verbose_name='名称'
    )
    
    level = models.IntegerField(
        choices=[
            (1, '省级'),
            (2, '地级市'),
            (3, '县/区'),
        ],
        verbose_name='层级'
    )
    
    parent = models.ForeignKey(
        'self',
        on_delete=models.CASCADE,
        null=True,
        blank=True,
        related_name='children',
        verbose_name='父级行政区划'
    )
    
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    
    class Meta:
        db_table = 'china_regions'
        verbose_name = '行政区划'
        verbose_name_plural = '行政区划'
        ordering = ['code']
    
    def __str__(self):
        return f'{self.name} ({self.code})'
    
    @property
    def full_path(self):
        """获取完整路径"""
        parts = [self.name]
        parent = self.parent
        while parent:
            parts.append(parent.name)
            parent = parent.parent
        return ' - '.join(reversed(parts))
```

**执行数据库迁移**：
```bash
./venv/bin/python manage.py makemigrations
./venv/bin/python manage.py migrate
```

---

### 步骤2：创建后端服务

**文件**：`core/services/china_region_service.py`

```python
from typing import List, Optional, Dict
import requests
from core.models import ChinaRegion


class ChinaRegionService:
    """中国行政区划服务"""
    
    DATA_SOURCE_URL = 'https://raw.githubusercontent.com/modood/Administrative-divisions-of-China/master/districts.json'
    
    # ===== 数据导入 =====
    
    @staticmethod
    def import_from_url(url: str = None) -> Dict:
        """从网络获取并导入数据"""
        url = url or ChinaRegionService.DATA_SOURCE_URL
        
        try:
            response = requests.get(url, timeout=30)
            response.raise_for_status()
            data = response.json()
            
            return ChinaRegionService._import_data(data)
        except Exception as e:
            return {'success': False, 'error': str(e)}
    
    @staticmethod
    def _import_data(data: List[Dict]) -> Dict:
        """解析并导入数据"""
        imported_count = 0
        
        for province_data in data:
            province = ChinaRegion.objects.update_or_create(
                code=province_data['code'],
                defaults={
                    'name': province_data['name'],
                    'level': 1,
                    'parent': None,
                }
            )[0]
            imported_count += 1
            
            # 导入城市
            for city_data in province_data.get('children', []):
                city = ChinaRegion.objects.update_or_create(
                    code=city_data['code'],
                    defaults={
                        'name': city_data['name'],
                        'level': 2,
                        'parent': province,
                    }
                )[0]
                imported_count += 1
                
                # 导入区县
                for district_data in city_data.get('children', []):
                    ChinaRegion.objects.update_or_create(
                        code=district_data['code'],
                        defaults={
                            'name': district_data['name'],
                            'level': 3,
                            'parent': city,
                        }
                    )
                    imported_count += 1
        
        return {'success': True, 'count': imported_count}
    
    # ===== 查询方法 =====
    
    @staticmethod
    def get_provinces() -> List[ChinaRegion]:
        """获取所有省份"""
        return list(ChinaRegion.objects.filter(level=1).order_by('code'))
    
    @staticmethod
    def get_cities(province_code: str) -> List[ChinaRegion]:
        """获取省份下的所有城市"""
        province = ChinaRegion.objects.filter(code=province_code, level=1).first()
        if not province:
            return []
        return list(province.children.filter(level=2).order_by('code'))
    
    @staticmethod
    def get_districts(city_code: str) -> List[ChinaRegion]:
        """获取城市下的所有区县"""
        city = ChinaRegion.objects.filter(code=city_code, level=2).first()
        if not city:
            return []
        return list(city.children.filter(level=3).order_by('code'))
    
    @staticmethod
    def get_by_code(code: str) -> Optional[ChinaRegion]:
        """根据代码获取行政区划"""
        return ChinaRegion.objects.filter(code=code).first()
    
    @staticmethod
    def search(keyword: str, limit: int = 20) -> List[ChinaRegion]:
        """搜索行政区划"""
        return list(ChinaRegion.objects.filter(
            name__icontains=keyword
        ).order_by('level', 'code')[:limit])
    
    @staticmethod
    def get_full_path(region_code: str) -> str:
        """获取完整路径（如：广东省-深圳市-南山区）"""
        region = ChinaRegion.objects.filter(code=region_code).first()
        if not region:
            return ''
        
        return region.full_path
    
    @staticmethod
    def get_tree() -> List[Dict]:
        """获取完整的树形结构"""
        provinces = ChinaRegion.objects.filter(level=1).order_by('code')
        
        result = []
        for province in provinces:
            province_data = {
                'code': province.code,
                'name': province.name,
                'level': 1,
                'children': []
            }
            
            for city in province.children.filter(level=2).order_by('code'):
                city_data = {
                    'code': city.code,
                    'name': city.name,
                    'level': 2,
                    'children': [
                        {'code': d.code, 'name': d.name, 'level': 3}
                        for d in city.children.filter(level=3).order_by('code')
                    ]
                }
                province_data['children'].append(city_data)
            
            result.append(province_data)
        
        return result
```

---

### 步骤3：创建 API 接口

**文件**：`core/urls.py`

```python
path('api/regions/provinces/', views.api_regions_provinces, name='api_regions_provinces'),
path('api/regions/cities/', views.api_regions_cities, name='api_regions_cities'),
path('api/regions/districts/', views.api_regions_districts, name='api_regions_districts'),
path('api/regions/search/', views.api_regions_search, name='api_regions_search'),
path('api/regions/path/', views.api_regions_path, name='api_regions_path'),
```

**文件**：`core/views.py`

```python
from django.http import JsonResponse
from django.views.decorators.http import require_GET
from core.services.china_region_service import ChinaRegionService


@require_GET
def api_regions_provinces(request):
    """获取所有省份"""
    provinces = ChinaRegionService.get_provinces()
    return JsonResponse({
        'success': True,
        'data': [{'code': p.code, 'name': p.name} for p in provinces]
    })


@require_GET
def api_regions_cities(request):
    """获取某省份的城市"""
    province_code = request.GET.get('province')
    if not province_code:
        return JsonResponse({'success': False, 'error': '缺少province参数'}, status=400)
    
    cities = ChinaRegionService.get_cities(province_code)
    return JsonResponse({
        'success': True,
        'data': [{'code': c.code, 'name': c.name} for c in cities]
    })


@require_GET
def api_regions_districts(request):
    """获取某城市的区县"""
    city_code = request.GET.get('city')
    if not city_code:
        return JsonResponse({'success': False, 'error': '缺少city参数'}, status=400)
    
    districts = ChinaRegionService.get_districts(city_code)
    return JsonResponse({
        'success': True,
        'data': [{'code': d.code, 'name': d.name} for d in districts]
    })


@require_GET
def api_regions_search(request):
    """搜索行政区划"""
    keyword = request.GET.get('q', '')
    if not keyword:
        return JsonResponse({'success': False, 'error': '缺少q参数'}, status=400)
    
    results = ChinaRegionService.search(keyword)
    return JsonResponse({
        'success': True,
        'data': [
            {
                'code': r.code,
                'name': r.name,
                'level': r.level,
                'full_path': r.full_path
            }
            for r in results
        ]
    })


@require_GET
def api_regions_path(request):
    """获取完整路径"""
    code = request.GET.get('code')
    if not code:
        return JsonResponse({'success': False, 'error': '缺少code参数'}, status=400)
    
    path = ChinaRegionService.get_full_path(code)
    return JsonResponse({
        'success': True,
        'data': {'code': code, 'path': path}
    })
```

---

### 步骤4：创建测试页面

**文件**：`core/urls.py`

```python
path('system/region-test/', views.region_test, name='region_test'),
```

**文件**：`core/views.py`

```python
@login_required
def region_test(request):
    """行政区划测试页面"""
    if not PermissionService.can_access_admin(request.user):
        messages.error(request, '需要系统管理员权限访问该页面')
        return redirect('core:dashboard')
    
    return render(request, 'core/region_test.html', {
        'active_section': 'region_test',
    })
```

**文件**：`templates/core/region_test.html`

```html
{% extends "core/frame_structure.html" %}

{% set show_nav = true %}
{% set show_header = false %}

{% block admin_title %}行政区划测试{% endblock %}

{% block admin_content %}
<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">三级联动选择</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label class="form-label">省份</label>
                    <select id="province" class="form-select">
                        <option value="">请选择省份</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label">城市</label>
                    <select id="city" class="form-select" disabled>
                        <option value="">请先选择省份</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label">区县</label>
                    <select id="district" class="form-select" disabled>
                        <option value="">请先选择城市</option>
                    </select>
                </div>
                <div class="mt-3">
                    <button id="getPath" class="btn btn-primary">获取完整路径</button>
                </div>
            </div>
        </div>
        
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="mb-0">选中结果</h5>
            </div>
            <div class="card-body">
                <p><strong>省份：</strong><span id="provinceName">-</span></p>
                <p><strong>城市：</strong><span id="cityName">-</span></p>
                <p><strong>区县：</strong><span id="districtName">-</span></p>
                <p><strong>完整路径：</strong><span id="fullPath">-</span></p>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">搜索功能</h5>
            </div>
            <div class="card-body">
                <div class="input-group mb-3">
                    <input type="text" id="searchInput" class="form-control" placeholder="输入关键词搜索...">
                    <button id="searchBtn" class="btn btn-outline-secondary">搜索</button>
                </div>
                <div id="searchResults" class="list-group">
                </div>
            </div>
        </div>
    </div>
</div>

<script>
$(function() {
    // 加载省份
    $.get('/api/regions/provinces/', function(res) {
        if (res.success) {
            res.data.forEach(function(p) {
                $('#province').append('<option value="' + p.code + '">' + p.name + '</option>');
            });
        }
    });
    
    // 省份变更
    $('#province').change(function() {
        var provinceCode = $(this).val();
        $('#provinceName').text($(this).find('option:selected').text());
        $('#city').empty().append('<option value="">请选择城市</option>');
        $('#district').empty().append('<option value="">请先选择城市</option>');
        
        if (provinceCode) {
            $.get('/api/regions/cities/', {province: provinceCode}, function(res) {
                if (res.success && res.data.length > 0) {
                    $('#city').prop('disabled', false);
                    res.data.forEach(function(c) {
                        $('#city').append('<option value="' + c.code + '">' + c.name + '</option>');
                    });
                } else {
                    $('#city').prop('disabled', true);
                }
            });
        } else {
            $('#city').prop('disabled', true);
        }
        $('#district').prop('disabled', true);
    });
    
    // 城市变更
    $('#city').change(function() {
        var cityCode = $(this).val();
        $('#cityName').text($(this).find('option:selected').text());
        $('#district').empty().append('<option value="">请选择区县</option>');
        
        if (cityCode) {
            $.get('/api/regions/districts/', {city: cityCode}, function(res) {
                if (res.success && res.data.length > 0) {
                    $('#district').prop('disabled', false);
                    res.data.forEach(function(d) {
                        $('#district').append('<option value="' + d.code + '">' + d.name + '</option>');
                    });
                } else {
                    $('#district').prop('disabled', true);
                }
            });
        } else {
            $('#district').prop('disabled', true);
        }
    });
    
    // 区县变更
    $('#district').change(function() {
        $('#districtName').text($(this).find('option:selected').text());
    });
    
    // 获取完整路径
    $('#getPath').click(function() {
        var code = $('#district').val() || $('#city').val() || $('#province').val();
        if (code) {
            $.get('/api/regions/path/', {code: code}, function(res) {
                if (res.success) {
                    $('#fullPath').text(res.data.path);
                }
            });
        }
    });
    
    // 搜索
    $('#searchBtn').click(function() {
        var keyword = $('#searchInput').val();
        if (keyword) {
            $.get('/api/regions/search/', {q: keyword}, function(res) {
                $('#searchResults').empty();
                if (res.success) {
                    res.data.forEach(function(r) {
                        $('#searchResults').append(
                            '<button class="list-group-item list-group-item-action" data-code="' + r.code + '">' +
                            r.name + ' <small class="text-muted">(' + r.full_path + ')</small></button>'
                        );
                    });
                }
            });
        }
    });
});
</script>
{% endblock %}
```

---

### 步骤5：数据导入命令（可选）

**文件**：`core/management/commands/import_china_regions.py`

```python
from django.core.management.base import BaseCommand
from core.services.china_region_service import ChinaRegionService


class Command(BaseCommand):
    help = '导入中国行政区划数据'
    
    def add_arguments(self, parser):
        parser.add_argument('--url', type=str, help='数据源URL')

    def handle(self, *args, **options):
        url = options.get('url')
        self.stdout.write('开始导入行政区划数据...')
        
        result = ChinaRegionService.import_from_url(url)
        
        if result.get('success'):
            self.stdout.write(self.style.SUCCESS(
                f'导入成功，共导入 {result.get("count", 0)} 条数据'
            ))
        else:
            self.stdout.write(self.style.ERROR(f'导入失败: {result.get("error")}'))
```

**用法**：
```bash
./venv/bin/python manage.py import_china_regions
```

---

## API 接口清单

| 接口 | 方法 | 参数 | 说明 |
|------|------|------|------|
| `/api/regions/provinces/` | GET | - | 获取所有省份 |
| `/api/regions/cities/` | GET | province | 获取某省份的城市 |
| `/api/regions/districts/` | GET | city | 获取某城市的区县 |
| `/api/regions/search/` | GET | q | 搜索行政区划 |
| `/api/regions/path/` | GET | code | 获取完整路径 |

---

## 文件清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `core/models.py` | ChinaRegion 模型 |
| `core/services/china_region_service.py` | 行政区划服务类 |
| `core/management/commands/import_china_regions.py` | 数据导入命令 |
| `templates/core/region_test.html` | 测试页面模板 |

### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `core/views.py` | API 视图、测试页面视图 |
| `core/urls.py` | 新路由配置 |

---

## 数据来源

- **数据源**：https://github.com/modood/Administrative-divisions-of-China
- **数据格式**：JSON
- **数据层级**：省 → 市 → 县/区

---

## 预计工作量

| 任务 | 工作量 |
|------|--------|
| 创建数据库模型 | 0.5小时 |
| 实现服务类 | 1小时 |
| 实现 API 接口 | 1小时 |
| 创建测试页面 | 1.5小时 |
| 测试验证 | 0.5小时 |
| **合计** | **约4.5小时** |
