# Node/Taxonomy 系统实现计划 - 总计划

## 一、系统概述

本系统用于在现有系统之上构建可自定义的数据模型，类似于 Drupal 的字段系统。

### 核心概念

| 概念 | 说明 |
|------|------|
| **Node（节点）** | 记录客户信息、项目信息等内容实体 |
| **NodeType（节点类型）** | 定义节点的字段结构 |
| **FieldType（字段类型）** | 预定义的字段类型（文本、数字、日期等） |
| **Taxonomy（词汇表）** | 下拉选项集合（如性别男/女） |
| **附加功能** | 节点类型管理、词汇表管理、数据分析等 |

---

## 二、执行顺序

本计划拆分为以下子计划，按顺序执行：

| 顺序 | 文件 | 说明 |
|------|------|------|
| **1** | `01_总计划.md` | 本文件 - 概述和执行顺序 |
| **2** | `02_现有功能迁移.md` | 将现有功能迁移到模块化架构 |
| **3** | `03_taxonomy模块.md` | 词汇表子系统设计与实现 |
| **4** | `04_字段模块.md` | 28 个字段类型模块设计与实现 |
| **5** | `05_客户信息范例.md` | 以"客户信息"为例的完整实现 |

---

## 三、模块化架构总览

```
app/
├── modules/                         # ← 所有业务模块（分子模块化）
│   ├── core/                      # 核心模块
│   │   ├── auth/                 # 认证子模块
│   │   ├── admin/                # 系统管理子模块
│   │   └── workspace/            # 工作台/仪表盘子模块
│   │
│   ├── profile/                   # 个人中心模块
│   ├── export/                   # 数据导出模块
│   ├── fields/                   # ← 字段类型子模块（28个字段）
│   └── taxonomy/                  # ← 词汇表子模块
│
├── routes/                        # 路由注册入口
├── services/                      # 公共服务
├── models.py                      # 数据模型
└── forms/                        # 公共表单
```

---

## 四、URL 结构总览

### 4.1 节点类型管理

```
/nodes/types              # 节点类型列表
/nodes/types/new          # 创建节点类型
/nodes/types/<id>/edit   # 编辑节点类型
/nodes/types/<id>/delete # 删除节点类型
```

### 4.2 词汇表管理

```
/nodes/taxonomies             # 词汇表列表
/nodes/taxonomies/new         # 创建词汇表
/nodes/taxonomies/<id>/edit  # 编辑词汇表
/nodes/taxonomies/<id>/delete # 删除词汇表
```

### 4.3 节点数据

```
/nodes/<type_slug>            # 节点列表
/nodes/<type_slug>/new        # 创建节点
/nodes/<type_slug>/<id>       # 查看节点
/nodes/<type_slug>/<id>/edit  # 编辑节点
/nodes/<type_slug>/<id>/delete # 删除节点
```

---

## 五、数据模型总览

| 模型 | 说明 |
|------|------|
| FieldType | 预定义字段类型（28种） |
| NodeType | 节点类型定义 |
| Node | 节点数据（JSON 存储） |
| Taxonomy | 词汇表 |
| TaxonomyItem | 词汇项 |

---

## 六、权限设计

| 权限键 | 说明 |
|--------|------|
| `node.types.manage` | 管理节点类型 |
| `node.taxonomy.manage` | 管理词汇表 |
| `node.<type_slug>.create` | 创建某类型节点 |
| `node.<type_slug>.read` | 读取某类型节点 |
| `node.<type_slug>.update` | 更新某类型节点 |
| `node.<type_slug>.delete` | 删除某类型节点 |

---

## 七、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-03-12 | 初始版本 - 拆分计划文件 |
